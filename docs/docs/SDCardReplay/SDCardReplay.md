# SD卡回放 (SDCard Replay)

SD卡回放流程 (SD card playback process)

1.传入当前零点零分的时间戳，获取SD中的视频文件列表

2.播放时查找视频播放时间 `videofiletime` 对应的视频文件进行播放

1.Pass in the current time stamp of zero minutes to get the list of video files in SD

2.Find the video playback time `videofiletime` during playback

